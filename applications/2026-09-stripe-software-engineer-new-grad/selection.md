# Selection — Stripe Software Engineer, New Grad

## NewsBreak title

**Software Engineering Intern**

JD phrase: "Software Engineer, New Grad" — general SWE, not backend / iOS / infra / AI-infra specific. Default print.

Header city: **Mountain View, CA** — JD lists New York, Seattle, and South San Francisco HQ; SF Bay / Mountain View wins when the posting allows it.

## Used

### NewsBreak

- **nb-fastapi-backend** (`detailed`) → bullet 1. Production APIs + offline tests + CI gate. Maps to Stripe's "most reliable APIs" and "safely update production systems". Skipped `nb-eks-standup` and `nb-from-scratch-stack` (conflicts). Did not use `with-cache` (would skip `nb-catalog-cache`).
- **nb-catalog-cache** (`redesign`) → bullet 2. First-principles cache redesign + caught OOM before production. Maps to rigor and safe production changes.
- **nb-llm-pipeline** (fusion of `shadow` + hand-graded, no sample size) → bullet 3. 4,100+ shadow rows, hand-graded alerts, 1.8 median/day cap 2, filler 34% → 4%. Maps to "leverage AI tools… rigorous critical thinking… validate all outputs". Did not print a hand-grade n, 89–90 scores, or device-weighted filler.
- **nb-ownership** (facts: 69% iOS, 83% backend, 344/340 PRs, sole full-time, onboarded next hire) → bullet 4. Large-codebase ownership + high agency. Did not repeat "opened the backend with its first commit" (already in bullet 1). Replaced `nb-jira-process` (weaker process/docs line).

### Haddee / ZenAI

- **hd-lead-mvp** (`default`) → lead an 8-intern 0→1 ship. Maps to "leading small projects independently".
- **hd-mentor-process** (`default`) → PR reviews + mentoring on Git / testing. Maps to "experience in code review practices".
- **za-mock-interview** (`default`) + **za-auth-db** (`default`) → two lines, one product.

### Research (fill — JD does not require it)

- **cm-psc** (`default`)
- **cm-nl-safety** (`default`)

### Projects / leadership

- **ld-ece-fellow** (`default`) — written / org communication.
- **pj-my-av** (`short`) — fill.

## Skipped

- **nb-eks-standup**, **nb-from-scratch-stack** — conflict with `nb-fastapi-backend`.
- **nb-tests-offline** — same story as fastapi-backend.
- **nb-jira-process** — real, but process/docs is weaker here than 344 PRs / sole-owner of a large codebase. Written comms still covered by ECE fellow.
- **nb-dramabreak-stripe** — real Stripe checkout, but thinner than APIs / cache / LLM rigor / ownership.
- **nb-dramabreak-ads**, **nb-swiftui-app**, **nb-server-driven-ui**, **nb-app-store**, **nb-live-activities**, **nb-ads-max**, **nb-morning-digest**, **nb-manual-broadcast** — mobile / ads, not this JD.
- **nb-third-party-proxy**, **nb-cronjob-outage**, **nb-retired-detector**, **nb-entity-resolution**, **nb-llm-failure**, **nb-agentic-search**, **nb-coaching-marketplace** — next-best; four NB bullets filled with higher-signal items.
- **za-demos-contracts** — growth-from-demos is weaker here than auth/product build.
- **pj-wisconsin-autonomous** — extra project; cut when the page spilled.

Coursework print: DS for SWE instead of Foundations of Software Engineering so the CMU line stays on one line. After inventory was used, itemsep / titlespacing were loosened to fill the bottom strip (helix-style 7pt / 1.2pt).

## Constraints almost violated

- Breaking-news send volume: printed 1.8 median/day, cap 2 — at the cap, not above.
- NewsBreak user growth: not printed as a solo claim.
- `nb-fastapi-backend` `with-cache` vs `nb-catalog-cache`: used `detailed` + separate cache bullet.
- `nb-llm-pipeline` do-not-claim A/B: did not call the rollout an experiment.
- `nb-llm-pipeline` do-not-claim hand-grade sample size: printed "hand-graded", not 67.
- Skills: only names from `inventory/skills.md`; Java / JavaScript / Go first because the JD lists them.
